<!DOCTYPE html>
<html>
  <head>
    <title>MarkingGenerator</title>
    <%= render "layouts/rails_defaults"%>
  </head>

  <body>
    <div class="container-fluid">
      <%= render "layouts/header"%>

      <div class ="jumbotron">
        <div class="text-center">
          <%= render "layouts/flash"%>

          <%= yield %>

          <%= debug(params) if Rails.env.development? %>
        </div>
       </div>
    </div>
  </body>
</html>
